

class InvertedIndex():
    '''This object acts as an inverted index...'''

    index_file_path
    dict_file_path
    posting_file_path

    def __init__(self, index_fp, dict_fp, posting_fp):
        '''Constructor...'''

        # If target files don't exist, create them
        return


    def build(self, posting):
        '''
        Build an inverted index given a list of term-document associations.i

        WARNING: This method will re-write the existing inverted index files!
        '''

        # Generate dictionary and posting objects

        # Write to files
        return
        

    def initalize_index(self):
        '''Pull the dictionary index into memory for effective searching.'''
        return
